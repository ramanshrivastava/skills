---
name: my-skill
description: One sentence on WHAT this skill does and WHEN to invoke it. Include the
  phrases a user would actually say (so the model auto-selects it). Keep it specific.
---

# My Skill

One-paragraph summary of the workflow this skill encodes and why it exists.

## When to use

- Concrete trigger 1
- Concrete trigger 2

## Steps

1. First step (reference any helper as `scripts/<name>.py`, relative to this skill's
   base directory — the runtime tells the agent the base dir at invocation).
2. Next step.

## Guardrails

- What NOT to do; safety/privacy notes; when to ask the user before acting.
